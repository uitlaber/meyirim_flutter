import 'package:flutter/material.dart';

class AddIndigent extends StatelessWidget {
  const AddIndigent({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
